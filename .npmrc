# Dependabot resolves lockfiles with GitHub's managed Node/npm versions.
# Workbench installs and builds enforce Node 22/npm 10 through the wrapper
# and explicit --engine-strict=true flags in Make, CI, and Docker.
engine-strict=false
