# Release Notes: v0.3.0

Historical note: this page records a VPW release-line milestone, not a reliable
current git-tag proof point. The repository contains inherited historical
`0.x` tags from earlier/template history. Verify exact tag state before using
any `0.x` name as release evidence.

## Highlights

- Added CTID Mappings Explorer JSON support for ATT&CK-aware vulnerability context.
- Added local ATT&CK technique metadata loading with tactic resolution and lifecycle flags.
- Upgraded `analyze`, `compare`, and `explain` with visible ATT&CK context and run summaries.
- Added `attack validate`, `attack coverage`, and `attack navigator-layer`.
- Added pinned ATT&CK fixtures, sample inputs, and example artifacts for the V0.3 workflow.

## Included Artifacts

Historical artifact names from the VPW release-line notes:

- `docs/example_attack_report.md`
- `docs/example_attack_compare.md`
- `docs/example_attack_explain.json`
- `docs/example_attack_coverage.md`
- `docs/example_attack_navigator_layer.json`

Do not treat inherited `0.x` tag links as current artifact evidence without an
exact tag check.

## Validation

- `python3 -m pytest -q`
- `make check`
- `make release-check`
- `python3 -m pre_commit run --all-files`

## Upgrade Notes

- The primary priority score is still based on CVSS, EPSS, and KEV.
- ATT&CK remains optional and local-file based.
- `--offline-attack-file` still works for the legacy CSV path.
- `--attack-source ctid-json` is the preferred V0.3 ATT&CK workflow.
