# Release Notes: v0.2.1

Historical note: this page records a VPW release-line milestone, not a reliable
current git-tag proof point. The repository contains inherited historical
`0.x` tags from earlier/template history. Verify exact tag state before using
any `0.x` name as release evidence.

## Focus

This patch release finishes the repository's maintainer-facing release surface after `v0.2.0`.

## Added

- `make package` for wheel-build verification
- `make release-check` for end-to-end local release validation
- GitHub PR template
- GitHub bug-report and feature-request issue templates

## Validation

- `make release-check`
- `python3 -m pre_commit run --all-files`

## Notes

- No prioritization rules changed in this patch release.
- The package version is now aligned with the latest published repository state.
